# Datadog API Key for sending metrics
# Get your API key from https://app.datadoghq.com/organization-settings/api-keys
DATADOG_API_KEY=your_datadog_api_key_here

# Database connection URL
# Format: postgres://username:password@hostname:port/database_name?sslmode=disable
DATABASE_URL=postgres://postgres:postgres@localhost:5432/yourdb?sslmode=disable

# Database type (postgres or mysql)
# Default is postgres if not specified
DATABASE_TYPE=postgres

# Optional: Enable debug mode to output detailed logs
# DEBUG=true
